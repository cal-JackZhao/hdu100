/*
 * p2024.cpp
 *
 *  Created on: Nov 4, 2018
 *      Author: zhaohankai
 */
#include<cstdio>
#include<cstring>
#include<string>
using namespace std;

bool valid(string s) {
	if (s[0]>='0' || s[0]<='9') return false;
	for (int i=0;i<strlen(s);i++){

	}
}

int main() {
	int n;
	string s;
	scanf("%d", &n);
	for (int i=0;i<n;i++) {
		scanf("%s", s);
		if (valid(s)) printf("yes\n");
		else printf("no\n");
	}
	return 0;
}



